server:
  port: 8761
security:
  basic:
    enabled: true
  user:
    name: root
    password: mopon2017
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
  instance:
    hostname: localhost